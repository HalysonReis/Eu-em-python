reais = float(input('quanto vale a hora de trabalho? '))
horas = float(input('quantas horas trabalhadas? '))

tot = reais * horas

salario = tot + (tot * 0.10)

print(f'o salario a ser pago é {salario}')